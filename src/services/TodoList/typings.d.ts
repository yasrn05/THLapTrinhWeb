declare module TodoList {
	export interface Record {
		description: ReactNode;
		color: any;
		title: ReactNode;
		// first: string;
		// last: string;
		// email: string;
		address: string;
		// created: string;
		// balance: string;
	}
}
